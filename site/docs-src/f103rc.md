# Generic F103RC — support notes

High-density STM32F103RC (256K flash, 48K RAM, LQFP64): the full F103
peripheral set. This is the chip where the modeled superset really
exists — **DAC, FSMC, ADC3, SDIO, UART4/5, SPI3, TIM5** — alongside
everything the Blue Pill has. DBGMCU IDCODE `0x10016410`.

## Silicon peripherals (delta vs Blue Pill)

- **DAC** (2ch, PA4/PA5) — modeled with DAC→ADC loopback wire
- **FSMC** (NOR/NAND/PC-Card) — modeled with MBKEN/WREN gating + backing images
- **ADC3** — same driver as ADC1/2 (SVD map; the hardcoded map carries ADC1/2)
- **SDIO**, **UART4/5** (`0x40004C00`/`0x40005000`), **SPI3** (`0x40003C00`), **TIM5**
- GPIOA–D (64-pin package; GPIOE–G exist only on 100+ pin parts)

## Board wiring (emulator rig)

- LED: board `LED_BUILTIN` (minimum boards usually PC13; the generic
  Arduino header leaves it undefined, so firmware uses the macro)
- `Serial` = **USART2** — like Nucleo, the input box follows via `uartAddr`

## Demos on this chip (29 presets)

All 24 shared presets, the RC builds (`board_rc`, `board_rc_echo`,
`board_rc_showcase`, `board_rc_rtc`) **plus the exclusive HD desk**
(`hd_fsmc`: FSMC NOR verify + dual-DAC loopback, `test_hd_fsmc.mjs` 5/5).
(USB/OTG presets show on every chip but only function where the
peripheral is mapped — see the matrix.)

## Verification

- periph39 firmware: **39/39** on `stm32f103rc`
- Real `GENERIC_F103RCTX` arduino-cli firmware boots + USART2-echoes
- `tests/test_board_demo.mjs`, `test_board_echo.mjs`,
  `test_board_showcase.mjs`, `test_board_rtc.mjs` green on `stm32f103rc`

## Feature matrix (silicon vs emulator)

Silicon = high-density STM32F103RC (256K flash, 48K RAM, LQFP64): the
full F103 peripheral set — this is the chip where the modeled superset
really exists. Emulator = this project's model on the builtin F103 map
(same map as every F103 chip: only flash/RAM/IDCODE differ). Statuses:
**Full** = register-level incl. IRQs/DMA/events; **Partial** = subset
(see remark); **Gap** = reads zero/lenient, no consumer; **SVD-map
only** = modeled, but not on the builtin map (wire via a custom SVD
chip object). Other labels (Driver/IDCODE only, Out of scope)
as marked. Depth: [PERIPHERALS](../PERIPHERALS.md).

| Feature | Silicon | Emulator | Remark |
|---|---|---|---|
| Cortex-M3 Thumb-2 (UDIV/SDIV, LDREX/STREX, TBB/TBH, unaligned) | Yes | Full | differential-fuzzed vs Unicorn oracle, 0 divergences |
| MPU (8 regions, subregions, AP/XN) | Yes | Full | enforced live on every access |
| Faults (MemManage/BusFault/UsageFault→HardFault, CFSR/BFAR/HFSR) | Yes | Full | SHCSR-gated escalation |
| SVC / PendSV, SHPR priorities, EXC_RETURN, PSP | Yes | Full | mini_rtos proof (preemptive PSP tasks) |
| SysTick (24-bit, debt drain) | Yes | Full | phase-exact multi-period re-pend |
| DWT CYCCNT | Yes | Partial | CYCCNT +1/latency from FLASH ACR; ITM/ETM/TPIU absent |
| Debug (SWD/JTAG, ETM trace) | Yes | IDCODE only | DBGMCU `0x10016410`; use the GDB stub instead |
| Printer hardware (TMC/TFT/heaters) | On SKR boards | Out of scope | SKR-class mechanics not modeled |
| RCC (HSI/HSE/PLL ×2–16, prescalers, CSS) | Yes | Full | clocks queryable (`rcc_clocks_hz`); MCO pin not modeled |
| FLASH 256K (program/erase, WRPRTERR) | Yes | Full | option bytes not modeled (no consumer) |
| PWR (PVD, Sleep/Stop/Standby) | Yes | Full | PVD→EXTI16; SLEEPDEEP freezes timers (RTC+IWDG keep running) |
| BKP (10 regs, tamper, RTC cal) | Yes | Full | TPE/TPAL edges + IRQ, W1C |
| RTC (second/alarm/overflow, PRL) | Yes | Full | 1 Hz PRL model; LSE/LSI assumed running |
| CRC-32 | Yes | Full | known-answer vectors |
| IWDG | Yes | Full | reset-request path |
| WWDG + EWI | Yes | Full | early-wakeup IRQ proven |
| 96-bit UID @0x1FFFF7E8 | Yes | Gap | reads zero; no consumer |
| GPIOA–D | Yes | Full | electrical model (pull-up/down, slew, open-drain, analog) |
| GPIOE (–G on 100+ pin) | E present | Full (A–E) | matches this package class |
| AFIO (remap, EXTI select) | Yes | Full | MAPR incl. TIM2/3/4 + CAN fixes; SWJ_CFG debug-disable not modeled |
| EXTI 0–15, 16/PVD, 17/RTC, 18/USB | Yes | Full | edge IRQs + ADC triggers; no line 19 (no ETH on F1) |
| DMA1 (7 ch, all requests) | Yes | Full | full request matrix (TIM/ADC/DAC/USART/SPI/I2C) |
| DMA2 (5 ch) | Yes | Full | incl. SDIO + TIM5–7 DAC paths |
| ADC1/ADC2 (SMP timing, AWD, temp sensor) | Yes | Full | Tconv-accurate, dual mode, injected + ext triggers |
| ADC3 | Yes (HD) | SVD-map only | same driver; not on the builtin F103 map (carries ADC1/2) |
| DAC 2ch + loopback | Yes | Full | PA4/PA5 wires + ADC loopback (`dac_sine`, HD desk) |
| TIM1 (advanced, BDTR/break) | Yes | Full | MOE/BKIN/LOCK; DTG stored |
| TIM2–4 (GP, capture, DMA burst, slave) | Yes | Full | input capture + AFIO remap, burst window, slave modes |
| TIM5 | Yes | SVD-map only | silicon has it; not on the builtin F103 map |
| TIM6/7 (basic) | Yes | Full | update IRQ + DMA requests |
| TIM8–14 | No F1 silicon | Driver only | instantiable for custom maps; out of scope |
| USART1–3 (TX pacing, LIN, errors) | Yes | Full | ORE SR→DR recovery, LIN SBK/LBD |
| UART4/5 (`0x40004C00`/`0x40005000`) | Yes | SVD-map only | silicon has them; not on the builtin F103 map |
| IrDA / smartcard / HDSEL | USARTs have it | Gap | pulse-shaping-invisible at register level; no consumer |
| SPI1/2 (+CRC-8/16) | Yes | Full | CRCNEXT phase + CRCERR |
| SPI3 (`0x40003C00`) | Yes | Full | on the builtin map |
| TI frame format (FRF) | SPIs have it | Gap | no consumer |
| I2S audio (via SPI2/3) | Yes | Full | I2SCFGR/I2SPR + audio-gen model |
| I2C1/2 (master/slave/10-bit/PEC/GENCALL) | Yes | Full | slave inject API; stretch-equivalent NACKs |
| SMBus ALERT | Yes | Full | both directions (CR1 drive + inject); ARP out of scope |
| CAN1 (loopback/silent, filters, TTCM) | Yes | Full | TX edge-trigger IRQ, timestamps |
| CAN2 | No (CL only) | SVD-map only | use the F105 map |
| USB FS device (EP0–7, SOF, suspend) | Yes | Full | BTABLE-16/PMA-1K/double-buffer/ISO/HP/PDWN/detach/DADDR |
| USB OTG_FS (device + host) | No (CL only) | SVD-map only | use the F105 map (119 unit + HCD e2e) |
| FSMC (NOR/NAND/PC-card) | Yes | Full | MBKEN/WREN gating + backing images + ECC accum (`hd_fsmc`) |
| SDIO (+MMC, DMA2) | Yes | Full | SDHC-only; DMA2 CH4 (`sd_logger`) |
| LED / Serial | `LED_BUILTIN` / USART2 | Full | input box follows via `uartAddr` |
